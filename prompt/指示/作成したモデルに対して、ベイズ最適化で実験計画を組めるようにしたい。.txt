作成したモデルに対して、ベイズ最適化で実験計画を組めるようにしたい。
カーネルはDotProduct + Matern + white などを基本とする. 

EI,Pi,PTRなどの設定をユーザーが実施する。

また、
一般的なベイズ最適化では何度もコンピューター上で計算を回すことが多いが、
今回はそうではなく、データの取得はオフラインで実施する。
また、１回で複数実験データを取得できることもあるので、ただし単純なベイズ最適化では上位候補が似たような候補だらけになることもある。
kriging-believerアルゴリズムを用いるとか、複数のベイズ候補点からさらに実験計画法を用いる、
実験計画法での候補から、ベイズ候補点を探す　などの工夫ができるようにしてほしい。

この辺はかなりインタラクティブ性が求められる。

また、既存実験点に対して、ベイズの候補がどのような空間にあるかを、２次元・３次元で常に可視化できるようにしたい。
PCAなど。　PCAを使う場合は常に累積寄与度とBIplotをもちいる。

またベイズ最適化の全探索範囲は、現実的な範囲で推奨を決めてほしい。　例えば数兆候補とかだと多すぎるし、
適度にありうる範囲で、グリッドもしくはグリッド＆ランダムして決める。

また、説明変数間の制約も入れられるようにしたい。例えばAとBとCの合計は100であるとか
Aはｘ以上ｙ以下　など。　ＡとＢとＣとＤのどちらか１つは必ず使うとか

そういう設定がユーザーがしやすいようなＵＩをロジカルに考えて。
制約の柔軟性を高めすぎると、ＵＩが見づらくなる。
まずＵＩが見やすい範囲でかける制約をかくＵＩを作り、
そのＵＩ範囲で設定できないものは別途制約をかけられるようにするとよいかも。

そのほか、ベイズ最適化を行う上で、ユーザーが使いやすいような工夫を実施せよ。
極めて重要な指示である。ユーザー目線のマネージャーをひとり専門に配置せよ

多目的ベイズ最適化についても論文等を調べて頑張れ